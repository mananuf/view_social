# =============================================================================
# VIEW SOCIAL BACKEND - ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# This file contains all environment variables required for the application.
# Copy this file to .env and fill in the appropriate values for your environment.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database connection URL
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://postgres:password@localhost:5432/view_social

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis connection URL for caching and session storage
# Format: redis://host:port or redis://username:password@host:port
REDIS_URL=redis://localhost:6379

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Secret key for signing JWT access tokens (CHANGE IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-in-production

# Secret key for signing JWT refresh tokens (CHANGE IN PRODUCTION!)
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port number for the HTTP server
PORT=3000

# Base URL for the application (used in email links, etc.)
BASE_URL=http://localhost:3000

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# SMTP server configuration for sending emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email sender information
FROM_EMAIL=noreply@viewsocial.com
FROM_NAME=VIEW Social

# =============================================================================
# SMS CONFIGURATION
# =============================================================================
# SMS provider configuration for sending verification codes
# Supported providers: termii, vonage, twilio, sendchamp
SMS_PROVIDER=termii

# SMS API credentials (required)
SMS_API_KEY=your-sms-api-key
SMS_API_SECRET=your-sms-api-secret

# SMS sender ID (optional, defaults to "VIEW")
SMS_SENDER_ID=VIEW

# SMS provider base URL (optional, uses provider defaults)
SMS_BASE_URL=

# =============================================================================
# DEVELOPMENT ENVIRONMENT
# =============================================================================
# Rust logging level (trace, debug, info, warn, error)
RUST_LOG=debug

# =============================================================================
# OPTIONAL CONFIGURATIONS
# =============================================================================
# These variables have defaults but can be overridden

# Application environment (development, staging, production)
# APP_ENV=development

# Maximum database connections
# DATABASE_MAX_CONNECTIONS=10

# Redis connection timeout (seconds)
# REDIS_TIMEOUT=5

# JWT token expiration time (seconds)
# JWT_EXPIRATION=3600

# Rate limiting configuration
# RATE_LIMIT_REQUESTS_PER_MINUTE=100
# AUTH_RATE_LIMIT_REQUESTS_PER_MINUTE=10

# File upload configuration
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=./uploads

# =============================================================================
# EXTERNAL SERVICE CONFIGURATIONS
# =============================================================================
# Add configurations for external services as needed

# Payment gateway configuration (when implemented)
# PAYMENT_PROVIDER=paystack
# PAYMENT_API_KEY=your-payment-api-key
# PAYMENT_SECRET_KEY=your-payment-secret-key

# Cloud storage configuration (when implemented)
# STORAGE_PROVIDER=aws-s3
# STORAGE_BUCKET=your-bucket-name
# STORAGE_ACCESS_KEY=your-access-key
# STORAGE_SECRET_KEY=your-secret-key

# Push notification configuration (when implemented)
# PUSH_NOTIFICATION_KEY=your-push-key
# FCM_SERVER_KEY=your-fcm-server-key